description:\nexpression: |
  params(Long :userId)
  {
    val :verifiedType = GetUser(:userId).safety.verified_type;

    if (:verifiedType != NULL) {
      ToLower(:verifiedType) == "government";
    } else {
      FALSE;
    }
  }
logged: 'true'
returnType: Auto
